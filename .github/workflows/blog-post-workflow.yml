name: Convert DEV Posts to Markdown
on:
 schedule:
     # At midnight twice a week on Monday and Fridayday
    - cron:  '0 * * * *'
 workflow_dispatch:
jobs:
  dev-to-md:
    runs-on: ubuntu-latest
    steps:
    - name: blog-post-workflow
      uses: neerajram30/neerajram30@master
    env:
      GITHUB_TOKEN: "${{ secrets.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 }}"
      DEV_API_KEY: "${{ secrets.iSACYixsYjg2bK3r7LEiLbxL }}"
      NUM_OF_POSTS: "${{ 5 }}"
